# copy this file to .env
# this default works as-is with the Docker database:
DATABASE_URL=postgresql://superstore:superstore@localhost:5433/superstore

# only needed for the chatbot, free key from aistudio.google.com:
GEMINI_API_KEY=your_key_here